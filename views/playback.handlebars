<header class="navigation">
	<h1>YouTube API How-To Guide</h1>
	<ul id="navigation_links">
		<li><a href="/how-to">Home</a></li>
		<li><a href="/common-issues">API Issues</a></li>
		<li><a href="/embed-player">Embed a YouTube</a></li>
		<li><a href="/playback">Playback Functions</a></li>
		<li><a href="/queuing">Queuing Functions</a></li>
	</ul>
</header>

<div class = "row">
	
	<div class="col-md-12">
		<h4>
			Below, you will find a list of functions to allow playback
			control on your embedded player. 
		</h4>
	</div>
	
	<div class="col-md-12">
		<h2>Playing a Video</h2>
		<p>
			These are the primary functions for video playback:
		</p>
		<ul id="indent">
			<li>
				<code>player.playVideo():Void</code> Plays the currently cued/loaded video..
			</li>
			<li>
				<code>player.pauseVideo():Void</code> Pauses the currently playing video unless the player is in the
				<code>ended</code> state.
			</li>
			<li>
				<code>player.stopVideo():Void</code> Stops and cancels loading of the current video. This function should be
				reserved for rare situations when you know that the user will not be watching additional video in the player. 
				If your intent is to pause the video, you should just call the <code>pauseVideo</code> function. 
				If you want to change the video that the player is playing, you can call one of the 
				queueing functions without calling stopVideo first.
			</li>
			<li>
				<code>player.seekTo(seconds:Number, allowSeekAhead:Boolean):Void</code> Seeks to a specified time in the video. 
				If the player is paused when the function is called, it will remain paused. 
				If the function is called from another state (playing, video cued, etc.), the player will play the video.
			</li>
		</ul>
	</div>
	
	<div class="col-md-12">
		<h2>Playing a Video in a Playlist</h2>
		<p>
			Videos that are part of a playlist have another set
			of functions to move through the playlist.
		</p>
		<ul id="indent">
			<li>
				<code>player.nextVideo():Void</code> This function loads and plays the next video in the playlist..
			</li>
			<li>
				<code>player.previousVideo():Void</code> This function loads and plays the previous video in the playlist.
			</li>
			<li>
				<code>player.playVideoAt(index:Number):Void</code> 
				This function loads and plays the specified video in the playlist.
			</li>
		</ul>
	</div>
	<div class="col-md-12">		
		<h2>Other Useful Functions</h2>
		<ul id="indent">
			<li><code>player.setVolume():Void</code> Sets the volume</li>
			<li><code>player.getVolume():Number</code> Returns player's current volume</li>
			<li><code>player.isMuted():Boolean</code> Returns <code>true</code> if the player is muted.</li>
			<li><code>player.mute():Void</code> Mutes the player.</li>
			<li><code>player.unMute():Void</code> Unmutes the player.</li>
		</ul>
		</br>
		<p  id="citation">
			Code derived from YouTube's documentation:
			<a href="https://developers.google.com/youtube/iframe_api_reference">
				https://developers.google.com/youtube/iframe_api_reference
			</a>.
		</p>
	</div>
</div>

<footer class="foot">
	@Sergio Pedroza<br>
  	Documentation produced by YouTube can be found at
	<a href="https://developers.google.com/youtube/">https://developers.google.com/youtube/</a>
</footer>